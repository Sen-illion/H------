# 网站还是打不开 — 按顺序查

## ① 本地 `docs` 里路径对不对？

用记事本打开 **`docs/index.html`**，看里面应是：

```html
src="/H------/assets/index-xxxxx.js"
```

若是 **`/assets/`**（前面没有 `/H------/`），说明构建没读到配置。请：

1. 确认存在 **`frontend/.env.github`**，且里面有 `VITE_BASE_PATH=/H------/`（仓库名按你的改）。
2. 在 **`frontend`** 里执行：`npm run build:pages:standalone`。

---

## ② 有没有推到 GitHub？

在项目根目录执行：

```bash
git status
```

若 **`docs/`** 下有修改且未提交，执行：

```bash
git add docs
git commit -m "更新 GitHub Pages"
git push origin main
```

只 `push`、不 `commit`，**线上不会变**。

---

## ③ GitHub 上是否真有这些文件？

浏览器打开（把仓库名换成你的）：

`https://github.com/Sen-illion/H------/tree/main/docs`

- 要有 **`index.html`**
- 要有 **`assets`** 文件夹，里面有 **`.js` 和 `.css`**

若 **`assets` 是空的** 或没有，说明没 push 全，再执行 `git add docs` 后提交推送。

---

## ④ Pages 是否指向 `docs`？

`Settings` → `Pages`：**Branch = main**，**Folder = /docs**（不要选根目录）。

---

## ⑤ 浏览器 / 网络

- **无痕窗口** 或 **Ctrl+Shift+R** 强制刷新。
- 若提示 **拒绝连接**，换 **手机热点** 或检查是否需 **代理/VPN** 访问 GitHub。
- 在 **`index.html`** 里点 **右键 → 在新标签打开** 脚本链接，看是 **200** 还是 **404**。

---

## ⑥ 仓库名必须一致

`.env.github` 里的 **`VITE_BASE_PATH`**、**`docs/index.html` 里的路径**、浏览器地址里的 **`github.io/仓库名/`** 三者必须是**同一个仓库名**。

{
  "name": "math-weekend-quiz",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build:server && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
